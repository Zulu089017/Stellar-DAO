[package]
name = "treasury"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Stellar Payment Gateway treasury contract — fee aggregation, asset deposits, admin-controlled withdrawals, and balance tracking"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true }
