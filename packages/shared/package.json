{
  "name": "@stellar-payment-gateway/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-package types, constants, source-chain config and env utils.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types/index.ts",
    "./constants": "./src/constants/index.ts",
    "./env": "./src/env/index.ts"
  },
  "scripts": {
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "pino": "^9.5.0",
    "pino-pretty": "^13.1.3",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@stellar-payment-gateway/eslint-config": "workspace:*",
    "@stellar-payment-gateway/tsconfig": "workspace:*",
    "typescript": "^5.6.3",
    "eslint": "^9.13.0",
    "vitest": "^2.1.9"
  }
}
